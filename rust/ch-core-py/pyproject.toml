[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "ch-core-py"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "_ch_core"
features = ["pyo3/extension-module"]
